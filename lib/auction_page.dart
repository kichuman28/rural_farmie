import 'package:flutter/material.dart';

class AuctionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Auction'),
      ),
      body: Center(
        child: Text('Auction Page Content'),
      ),
    );
  }
} 